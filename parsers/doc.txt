project/
├── parsers/
│   ├── __init__.py
│   ├── network_traffic.py -> содержит класс для автоматизированной работы с браузером для получения cookies и токенов
│   │                         доступа из источников парсинга
│   ├── edostavkaby/ -> пакет для скрапинга одноимённого интернет-магазина
│   │   ├── __init__.py
│   │   ├── controller.py -> основной модуль, внутри которого осуществляется создание экземпляров лассов для парсинга и базы
                            данных а так же из запуск и совместную работу с обменом данными.
│   │   ├── schemas.py -> классы pydantic для парсинга json документов ответа сайта
│   │   ├── spider_sync.py -> основной класс парсинга сайта, содержащий синхронные методы работы. crawl функция содержит
    │   │                     логику обхода сайта-источника и порционный возврат готовых данных во внешнюю область (controller)


[Unit]
Description=AI chat bot
After=network.target

[Service]
Type=simple
User=amnesia
Group=amnesia

# Рабочая директория (где находится ваш скрипт)
WorkingDirectory=/home/amnesia/tg_bots/ai_chat_bot_tg

# Команда для запуска
# Активируем venv и запускаем скрипт
ExecStart=/home/amnesia/tg_bots/ai_chat_bot_tg/venv/bin/python /home/amnesia/tg_bots/ai_chat_bot_tg/bot.py

# Перезапуск при сбое
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target